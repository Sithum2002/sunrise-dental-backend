spring:
  datasource:
    url: jdbc:mysql://mysql-2047767e-sithumasam-6934.h.aivencloud.com:16606/SunriseDentalDB?useSSL=true&requireSSL=true&sslMode=REQUIRED&serverTimezone=Asia/Colombo
    username: avnadmin
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
  jpa:
    show-sql: true
  sql:
    init:
      mode: never

app:
  mail:
    enabled: ${MAIL_ENABLED:false}
    from: Sunrise Dental Clinic <info@sunrisedental.lk>
  sms:
    enabled: ${SMS_ENABLED:false}
    gateway: Simulator
  seed:
    enabled: true
